<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <style>
        body {
            background-color: #F0F0F0;
            font-family: 'Arial', sans-serif;
            color: #333;
            margin: 0;
            padding: 40px;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        h1 {
            font-size: 48px;
            text-align: center;
            margin-bottom: 40px;
            color: #2C3E50;
            font-weight: 700;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }

        .post {
            background-color: #FFFFFF;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 20px;
            width: 100%;
            max-width: 700px;
            transition: box-shadow 0.3s ease;
        }

        .post:hover {
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        }

        .username {
            font-weight: 600;
            font-size: 18px;
            color: #333;
            margin-bottom: 10px;
        }

        .content {
            font-size: 16px;
            color: #555;
            line-height: 1.6;
            margin-bottom: 15px;
        }

        .button-container {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
        }

        button {
            background-color: #333;
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 5px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            text-transform: uppercase;
            transition: background-color 0.3s ease, transform 0.2s ease;
            margin: 0 5px;
            flex: 1;
            max-width: 150px;
            white-space: nowrap;
        }

        button:hover {
            background-color: #555;
            transform: translateY(-2px);
        }

        .create-new {
            margin-top: 30px;
            padding: 12px 20px;
            font-size: 16px;
            background-color: #007BFF;
            border-radius: 5px;
            color: white;
            font-weight: 600;
            white-space: nowrap;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            max-width: 200px;
        }

        .create-new:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        form button {
            background-color: #FF3B30;
        }

        form button:hover {
            background-color: #D32F2F;
        }

        hr {
            width: 100%;
            max-width: 700px;
            border: none;
            height: 1px;
            background-color: #E1E1E1;
            margin: 30px 0;
        }

        .logo {
            font-size: 36px;
            font-weight: bold;
            background: -webkit-linear-gradient(#ff7e5f, #feb47b);
            -webkit-background-clip: text;
            background-clip: text; /* Fallback for non-WebKit browsers */
            -webkit-text-fill-color: transparent; /* Transparent text to show gradient */
            display: inline-block;
        }
    </style>
</head>
<body>
    <div class="logo">LinkUp</div>

    

    <h1>All Posts</h1>

    <hr>

    <% for(let post of posts) { %>
        <div class="post">
            <div class="username">Username: @<%= post.username %></div>
            <div class="content">Content: <%= post.content %></div>
            <div class="button-container">
                <button onclick="window.location.href='http://localhost:8080/posts/<%=post.id%>'">View</button>
                <button onclick="window.location.href='http://localhost:8080/posts/<%=post.id%>/update'">Update</button>
                <form method="post" action="posts/<%= post.id%>?_method=DELETE">
                    <button>Delete</button>
                </form>
            </div>
        </div>
    <% } %>

    <button class="create-new" onclick="window.location.href='http://localhost:8080/posts/new'">Create New Post</button>

</body>
</html>
