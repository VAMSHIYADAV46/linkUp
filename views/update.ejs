<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Post</title>
    <style>
        body {
            background-color: #F0F0F0;
            font-family: 'Arial', sans-serif;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
            flex-direction: column; /* Ensures logo is above the form */
        }

        form {
            background-color: #FFFFFF;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            transition: box-shadow 0.3s ease;
        }

        h1 {
            text-align: center;
            color: #2C3E50;
            margin-bottom: 20px;
            font-weight: 700;
            font-size: 24px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .post {
            margin-bottom: 20px;
            padding: 10px;
        }

        .username {
            font-size: 16px;
            color: #555;
        }

        .content {
            font-size: 16px;
            color: #555;
            padding: 10px;
            background-color: #E9EED9;
            border-radius: 6px; 
            border: 1px solid #789DBC; 
            margin: 10px 0; 
            white-space: pre-wrap;
        }

        label {
            font-weight: bold;
            color: #789DBC;
            display: block;
            margin: 20px 0 6px 10px;
        }

        textarea {
            width: 96%;
            padding: 12px;
            margin-bottom: 20px;
            margin-left: 10px;
            border: 2px solid #789DBC;
            border-radius: 6px;
            background-color: #F9F9F9;
            color: #333;
            font-size: 16px;
            box-sizing: border-box;
            outline: none;
            transition: border 0.3s ease;
        }

        textarea:focus {
            border-color: #567799;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        button:hover {
            background-color: #0056b3;
        }

        button:active {
            transform: scale(0.98);
        }

        .logo {
            font-size: 36px;
            font-weight: bold;
            background: -webkit-linear-gradient(#ff7e5f, #feb47b);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 30px; /* Space between logo and form */
        }
    </style>
</head>
<body>

    <div class="logo">LinkUp</div>

    <form method="POST" action="/posts/<%=post.id%>?_method=PATCH">
        <h1>Update Post</h1>
        <div class="post">
            <p>Post ID: <%=post.id%></p>
            <div class="username">Username: @<%=post.username%></div>
            <div class="content">Content: <%=post.content%></div>
        </div>

        <label for="content">New Content:</label>
        <textarea name="content" id="content" placeholder="Enter new content" required></textarea>

        <button type="submit">Submit</button>
    </form>
</body>
</html>
